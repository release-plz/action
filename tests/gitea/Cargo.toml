[package]
name = "gitea-integration-test"
version = "0.1.0"
edition = "2024"
publish = false

[dev-dependencies]
anyhow = "1"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json"] }
serde_json = "1"
tempfile = "3"
